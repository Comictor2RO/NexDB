============================================================
1) Mini Database Engine (gen SQLite Lite)
============================================================

Un proiect excelent pentru CV, pentru că arată:
    • structuri de date avansate
    • gestionarea memoriei
    • parsing
    • optimizări
    • graphics programming (RayLib GUI)

============================================================
PROGRES
============================================================

[✓] IMPLEMENTAT:
    • AST complet
        - Statement (baza abstracta)
        - SelectStatement
        - InsertStatement
        - DeleteStatement
        - CreateStatement
        - Condition
        - Columns
        - DropStatement
        - UpdateStatement
    • Lexer
        - TokenType, Token (fisiere separate)
        - Tokenizare completa: keywords, identifiers,
          numbers, strings, operators, punctuation, wildcard
    • Parser
        - parseSelect, parseInsert, parseDelete, parseCreate
        - parseCondition, parseDrop, parseUpdate
        - validare query invalid
    • Storage
        - PageHeader
        - Page (addRow, getRows, hasSpace, getRowCount)
        - PageManager (readPage, writePage, insertRow, getAllRows)
    • Catalog
        - createTable, tableExists, getColumns, dropTable
        - persistență: schema salvată în catalog.dat, încărcată la pornire
        - la load(): se păstrează doar tabelele al căror fișier .db există;
          dacă ștergi emp.db, la repornire emp dispare din catalog și din catalog.dat
          (nu se mai recreează emp.db la SELECT * FROM emp)
    • Table
        - leaga Catalog + PageManager
        - API simplu pentru operatii pe tabele (insert/select/delete/drop/update)
    • Engine
        - executa Statements peste Table/PageManager
        - leaga parser-ul de storage (end-to-end DB functional)
    • B+ Tree Index
        - IndexRecord (key, pageId, offset)
        - BPlusNode (isLeaf, keys, children, records, next, parent)
        - BPlusTree (insert, search, clear, splitLeaf, splitInternal)
        - integrat in Table: cautare O(log n) pe prima coloana INT
        - rebuild automat al indexului la crearea obiectului Table
        - reset + rebuild la DELETE
    • Page Cache LRU
        - CacheEntry (page, isDirty)
        - LRUCache (get, put, evict, clear, flushAll)
        - std::list pentru ordinea LRU + std::unordered_map pentru O(1) lookup
        - integrat in PageManager: readPage verifica cache inainte de disc
        - writePage scrie in cache cu isDirty=true, nu direct pe disc
        - flushAll() in destructorul PageManager garanteaza persistenta
        - clear() folosit in clearAll() pentru reset fara scriere pe disc

[✓] Write-Ahead Logging (WAL)
        - WALEntry (type, table, rowData, committed)
        - WALManager (logInsert, commit, recover, readLog)
        - integrat in Engine: logInsert inainte de INSERT, commit dupa
        - recover() apelat automat in constructorul Engine la pornire
        - testat: date recuperate corect dupa crash simulat
    • RayLib GUI
        - fereastra 1280x720, 3 panouri: input, results, log
        - input cu cursor (sageti stanga/dreapta), Backspace cu repeat la hold
        - buton RUN + Enter pentru executie query
        - tema dark/light (buton in log panel)
        - erori afisate in log, rezultate SELECT in grid

============================================================
NEXT TO DO
============================================================
Memory Management
Înlocuiește raw new/delete cu std::unique_ptr<BPlusNode> în B+Tree/Table pentru auto-cleanup și evitare leaks/UAF.
Adaugă RAII wrappers pentru fișiere (ex: std::unique_ptr<FILE, fclose_deleter>) în PageManager/WAL.

Error Handling & Validation
Wrap toate std::stoi în try-catch; returnează std::optional<int> sau custom Result<T,Err> în loc de throw.
Parser: Gestionează erori graceful (ex: "Unexpected token at pos X") cu std::expected (C++23) sau variant; validează schema strict (no empty cols).

Concurrency & Thread-Safety
Adaugă std::mutex în Table/PageManager pentru multi-thread (ex: GUI + queries); folosește lock guards.
WAL: Atomic writes cu fsync pentru durability reală.

Testing & Robustness
Scrii unit tests (GoogleTest) pentru B+Tree (insert 1M keys, splits), Parser (malformed SQL), full CRUD cycles.
Valgrind/ASan pentru detect leaks/OOB; fuzzing pe Lexer cu random input.

Alte Îmbunătățiri
Configurabil: PAGESIZE, B+ order via flags; LRU cache mai agresiv.
Logging: spdlog pentru trace/debug; metrics (query time, hit rate).

GUI
Font custom – LoadFontEx / DrawTextEx peste tot (îl menționaseși).
Istoric query – săgeți sus/jos în input pentru comenzi anterioare.
Highlight SQL – culori diferite pentru keywords, stringuri, numere în zona de input.

Dezvoltare și CV
Teste automate – un singur executable de teste (fără GUI) care rulează CREATE/INSERT/SELECT/DELETE (și UPDATE/DROP dacă le adaugi) și verifică rezultatele; util și pentru regresii.
Export CSV – buton/shortcut în GUI: exportă rezultatele SELECT curent într-un .csv